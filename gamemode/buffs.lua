BUFFS["Bleeding"] = {
	Color = Color(205, 55, 55, 255),
	OnReceive = function(pl, limb)
		
	end,
	Icon = Material("icons/notif_bleeding.png")
}

BUFFS["Broken"] = {
	Color = Color(155, 55, 55, 255), 
	OnReceive = function(pl, limb)
	
	end,
	Icon = Material("icons/notif_injury.png")
}